# Заметки по ходу

Черновик: решения, которые принимались по дороге, и то, что не влезло в
README и CHANGELOG.

## Базовая версия v15

- Код собран из пятнадцати упражнений, но не импортом, а переносом с правкой
  по существу: у каждого прошлого упражнения свой модуль, и связь через
  `replace` сделала бы репозиторий несамодостаточным.
- Основа — упражнение 15 (слои памяти, профиль, свод, жизненный цикл), к ней
  доложены трекер вызовов и команда специалистов из 06, ветки и карточка
  фактов из 10, привратник из 02.
- С нуля — `store` (версии файлов), `features` (реестр механизмов), `card`,
  `bench`.
